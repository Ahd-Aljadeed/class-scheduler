{
  "name": "unischedule-planner",
  "private": true,
  "version": "1.0.0",
  "description": "A client-side university class scheduler that finds conflict-free timetables and reports gap hours, campus time and days off.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "test": "node --test \"test/**/*.test.js\"",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  }
}
